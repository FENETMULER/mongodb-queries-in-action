{
  "name": "mongodb-crud",
  "version": "1.0.0",
  "description": "A list of CRUD operations using mongoose and MongoDB",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "mongoose": "^7.0.0"
  }
}
